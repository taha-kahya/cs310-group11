import 'package:flutter/material.dart';

// TODO: These colors are determined arbitrarily for now, update here after the colors are decided
class AppColors {
  static const primary = Color(0xFF333333);    // dark gray for headers, main text
  static const secondary = Color(0xFF666666);  // medium gray for secondary text
  static const background = Color(0xFFF9F9F9); // light gray for background
  static const surface = Color(0xFFFFFFFF);    // white for cards, panels
  static const accent = Color(0xFF007BFF);     // subtle blue for buttons/links
}
